# vi: ft=python
from pyasm import *

humanize = load(nid="humanize", uri=uri(name="humanize", scope=pack("native")))
w = const(word(), t(351, 0))
ap = apply(func=humanize, arg=w, nid="apply-w", terminal=True)

encode([
    graph([humanize, w, ap, ap], -1, identity=uri("call-things")),
])

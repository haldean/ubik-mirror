# vi: ft=python
from pyasm import *

n_nodes = 20000
tree = t(0x1234567890123456, t(66, 0xBADDEADBEEF))
g = graph(
    [const(uint8(), tree)] * (n_nodes - 1) +
        [const(uint8(), tree, terminal=True)],
    -1)

encode([g], expect=tree)

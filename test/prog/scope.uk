~ ubik-tests/scope

: so-deep
    ^ Word -> (Word -> Word)
    = \ x -> {
        : y ^ Word = rational-add x 2
        ! \ z -> rational-add x (rational-add y z)
    }

# + 4 (+ (+ 4 2) 8) == 18
! emit (humanize ((so-deep 4) 8))

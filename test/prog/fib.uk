~ ubik-tests/fib

: fib
    ^ Number -> Number
    = \x -> ? {
        . eq x 0 => 1
        . eq x 1 => 1
        .        => rational-add (fib (rational-subtract x 1))
                                 (fib (rational-subtract x 2))
    }

! emit (concat (humanize (fib 5)) "\n")

reference tracking for graphs
runtime instrumentation
type-based dispatch node
clang format or whatever
where did a graph come from
callback-based scheduler
tuples
don't need value_type, just use tag of child
lambda types
remove value type from env, replace with tag
